import { createSeoMetadata } from '@/lib/seo';
export const metadata = createSeoMetadata({ title: 'Edit Career Role | Ro-Link Management', description: 'Edit a Ro-Link career listing and its application questions.', index: false });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
