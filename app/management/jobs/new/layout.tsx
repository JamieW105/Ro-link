import { createSeoMetadata } from '@/lib/seo';
export const metadata = createSeoMetadata({ title: 'New Career Role | Ro-Link Management', description: 'Create a new Ro-Link career listing and application form.', index: false });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
