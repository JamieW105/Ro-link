import { createSeoMetadata } from '@/lib/seo';
export const metadata = createSeoMetadata({ title: 'Edit Product Update | Ro-Link Management', description: 'Edit and publish a Ro-Link product update.', index: false });
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
